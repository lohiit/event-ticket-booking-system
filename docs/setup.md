# Local setup checklist

1. Copy `.env.example` to `.env` and enter local MySQL credentials.
2. Create the `event_ticket_booking` database.
3. Run the SQL files in the order documented in `db/README.md`.
4. Configure a local PHP server with `public/` as its document root.
